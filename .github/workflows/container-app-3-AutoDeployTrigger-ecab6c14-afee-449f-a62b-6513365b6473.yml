name: Trigger auto deployment for container-app-3

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ azure-app-insigths ]
    paths:
    - 'azure-app-insigths/with-java-agent/**'
    - '.github/workflows/container-app-3-AutoDeployTrigger-ecab6c14-afee-449f-a62b-6513365b6473.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to container registry
        uses: docker/login-action@v1
        with:
          registry: artus1containerregistry.azurecr.io
          username: ${{ secrets.CONTAINERAPP3_REGISTRY_USERNAME }}
          password: ${{ secrets.CONTAINERAPP3_REGISTRY_PASSWORD }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: artus1containerregistry.azurecr.io/container-app-3:${{ github.sha }}
          file: ./azure-app-insigths/with-java-agent/Dockerfile
          context: ./azure-app-insigths/with-java-agent/


  deploy:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.CONTAINERAPP3_AZURE_CREDENTIALS }}


      - name: Deploy to containerapp
        uses: azure/CLI@v1
        with:
          inlineScript: |
            az config set extension.use_dynamic_install=yes_without_prompt
            az containerapp registry set -n container-app-3 -g resource-group-container-app-1 --server artus1containerregistry.azurecr.io --username  ${{ secrets.CONTAINERAPP3_REGISTRY_USERNAME }} --password ${{ secrets.CONTAINERAPP3_REGISTRY_PASSWORD }}
            az containerapp update -n container-app-3 -g resource-group-container-app-1 --image artus1containerregistry.azurecr.io/container-app-3:${{ github.sha }}
